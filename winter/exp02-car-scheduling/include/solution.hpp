#pragma once
#include <iostream>
#include <vector>

bool runTask01(const std::vector<int>& outputOrder, int numOfCars,
            std::ostream* out = &std::cout);

bool runTask02(const std::vector<int>& inputOrder, int numOfCars,
               std::ostream* out = &std::cout);