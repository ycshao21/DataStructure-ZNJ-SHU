#pragma once
#include <iostream>
#include <vector>

bool task01(const std::vector<int>& outputOrder, int numOfCars,
            std::ostream* out = &std::cout);

bool task02(const std::vector<int>& inputOrder, int numOfCars,
            std::ostream* out = &std::cout);
