#pragma once

#include <iostream>
#include <vector>

void findMinCommonElement(const std::vector<int>& a, const std::vector<int>& b,
                          const std::vector<int>& c, std::ostream* out = &std::cout);