#pragma once

#include <iostream>
#include <string>
#include <vector>

std::vector<std::string> dnaSort(const std::vector<std::string>& dnaVec,
                                 std::ostream* out = &std::cout);
