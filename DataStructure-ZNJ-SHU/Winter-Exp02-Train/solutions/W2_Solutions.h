#pragma once
#include<iostream>
#include<vector>

namespace W2
{
    bool Question1(const std::vector<int>& outputOrder, int numOfCars, std::ostream* out = &std::cout);
    bool Question2(const std::vector<int>& inputOrder, int numOfCars, std::ostream* out = &std::cout);
}
